version: '3.8'
services:
  motofacil-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: motofacil-api
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    # volumes: # Descomente se quiser mapear código fonte ou volumes
    #   - ./MotoFacil.API-devops:/app